[
  {"id": 1, "desc": "Reproduce + confirm mechanism (no code change): baseline screenshot with ENGINE_IBL_DISABLED=1 ENGINE_AZGAAR_WEATHER=0 ENGINE_TPOSE=1 ENGINE_HIDE_GUI=1; A/B with ENGINE_BRIX_GI_DIFFUSE_FACTOR=0.3/SPECULAR_FACTOR=0.3; optional ENGINE_BRIX_GI_SAVE dumps. Confirm washed-out look and over-addition", "status": "done", "result": "Washed-out confirmed in /tmp/gi_before.jpg; A/B at factor 0.3 restored contrast (wall lum 108->70); raw GI dumps show unweighted env irradiance equal on dark/light surfaces; specular GI all-black; ramp fills by frame ~20"},
  {"id": 2, "desc": "Inspect Brixelizer GI pass + composite to confirm root cause (raw radiance added without albedo/F0 weighting; feedback loop gain ~1 via prevLitOutput)", "status": "done", "result": "Confirmed: composite adds raw GI radiance with no albedo/F0 weighting (composite.comp:139-147); feedback loop gain ~1 via prevLitOutput; specular GI zero in scene; wash-out is diffuse term"},
  {"id": 3, "desc": "Implement fix for washed-out GI colors (parent of tasks 5 and 6)", "status": "done", "result": "Complete via tasks 5 (albedo GBuffer) + 6 (composite weighting); both verifier PASS"},
  {"id": 4, "desc": "End-to-end verification: rebuild, re-run exact user recipe to /tmp/gi_after.jpg, compare vs baseline (contrast/saturation restored, no new artifacts)", "status": "done", "result": "Satisfied by verifier id 6 run: exact user recipe (IBL off, weather off, T-pose, parked camera) -> /tmp/verify_composite.jpg shows restored contrast/saturation vs washed-out baseline; wall (109,108,103)->(90,49,43); no new artifacts; parked db untouched"},
  {"id": 5, "desc": "Add albedo GBuffer attachment: frame-resource image + getter, colorFormat4 on scene/terrain/props pipes, clear in terrain clear-first path, albedo out in scene.frag / heightmap_terrain.frag / azgaar_props.frag", "status": "done", "result": "Albedo R16G16B16A16_SFLOAT frame image + getter; colorFormat4 on scene/terrain/props pipes (terrain clear-first); outAlbedo in scene/heightmap_terrain/azgaar_props frags; albedo dump token; VERIFIER PASS (build warning-free, clean run, dump shows correct per-surface albedo)"},
  {"id": 6, "desc": "Weight GI in composite.comp: giD * albedo*(1-metallic)*factor, giS * (F0*brdf.x+brdf.y)*factor via brdfLutIndex with F0-only fallback; keep absent-sentinel path bit-identical", "status": "done", "result": "albedoIndex push constant + split-sum weighting in composite.comp; albedo layout transition in VulkanCompositePass.cpp (restore COLOR_ATTACHMENT_OPTIMAL after dispatch); sentinel fallback bit-identical; VERIFIER PASS (visual fix confirmed on exact user recipe)"}
]
