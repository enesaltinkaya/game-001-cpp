[
  {"id": 1, "desc": "P1b: full hemisphere ray-march estimate shader (gi_estimate.comp, 6 rays, SSR-convention linear march, analytic hit radiance, confidence)", "status": "done", "result": "Landed R2, verified PASS. 0.94ms @6 rays (linear ~0.152ms/ray+0.06 fixed; 4 rays=0.65ms). Over 0.2 target, under 1.5 fail line. DepthPass albedo layout restore needed (VUID 09592). No sky texels at parked vantage."},
  {"id": 2, "desc": "P2: gi_temporal.comp + ping-pong giHistoryA/B + ENGINE_GI_T* knobs (Phase 2, D4)", "status": "done", "result": "Landed R3, verified PASS. Pass total 1.13ms (0.94 est + 0.19 temporal, under 1.5 fail line). Boiling: 6.8x inter-frame diff reduction, 0 NaN. History rgb=irradiance a=confidence + separate R16F inv-depth ping-pong (channel-layout deviation from D2 5-values/4-channels, resolved sensibly)."},
  {"id": 3, "desc": "P3: sceneBuffer giIndex + ambient injection in scene.frag/azgaar_props.frag + AO attenuation + settings (Phase 3, D5-D6)", "status": "done", "result": "Landed R4 (with task 7), verified PASS. GiData in SceneBuffer + one-frame-latency publish + sentinel; energy contract per lemma 1 (no shadowDarkFactor in mix); AO_SCALE attenuation crevice-localized (A/B verified numerically: 51% pixels >2/255 changed); giDisabled settings off-by-default + GUI toggle. Per-frame index writes must patch current flight index only."},
  {"id": 4, "desc": "P4: validation screenshots, GPU cost re-baseline, reactive-mask check, RADV flicker check, docs updates (fsr3.1.md reactive section, global-illumination.md status), ray-count budget call, purge stale gi* keys in local settings.json, flip settings default. Docs-only flips last", "status": "done", "result": "Landed R5, verified PASS. Reactive mask unaffected (GI reduces mean 0.110->0.094, boiling unchanged); keep 6 rays (gi pass 1.14ms, under 1.5 fail line); RADV/OIT/vegetation checks clean; docs updated; stale keys purged; default flipped GI-on."},
  {"id": 5, "desc": "P0: energy-contract addendum — pin units (cosine-weighted irradiance), fixed injection equation, IBL fallback chain, before any shader code (doc-only)", "status": "done", "result": "Pinned in notes.md ## brainstorm lemma 1: estimate=cosine-weighted irradiance (x pi), consumer=mix(ambientDiffuse_ibl, kD_ibl*gi.rgb*baseColor/PI*iblIntensity, gi.a*giIntensity), NO shadowDarkFactor inside mix"},
  {"id": 6, "desc": "P1a: VulkanGiPass skeleton registered SSR->AO + constant-sky estimate + giEstimate/gi dump tokens + ENGINE_GI_DISABLED path", "status": "done", "result": "Landed R1: pass+pipe+half-res image+dumps+disable path; gi=0.01ms GPU; uniform dump (0.752,1.038,1.390,1.0) verified via Raw token (JPEG normalizes uniform buffers to black)"},
  {"id": 7, "desc": "Standalone: vulkanAOPassSetStrength() override member beating per-frame ENGINE_AO_STRENGTH read", "status": "done", "result": "Landed R4 as P3 prerequisite, verified PASS with the round."}
]
