[
  {"id": 1, "desc": "Read docs/global-illumination.md in full; extract the verified engine facts (pass order, G-buffer slot/format table, Forward+ config, AO temporal-pass pattern, FSR 3.1 reactive-mask risk, GTX 1080 Ti cost budget) into notes.md with file citations.", "status": "done", "result": "Brainstorm extracted and re-verified the facts into notes.md with file/line citations (Vulkan.cpp:293-323, VulkanAOPass.cpp:258, scene.frag:195-227, azgaar_props.frag:171-180, composite.comp:116-120); corrected two stale survey claims (AO strength knob exists; gi_estimate.spv present)."},
  {"id": 2, "desc": "Map each SSGI++ stage (hemisphere ray tracing, visibility-weighted irradiance accumulation, temporal filter with depth/normal edge continuity, spatial blur, ambient-term injection) to a concrete engine change: new pass name and insertion point (after oit_composite, before ao), new RTs + formats at FSR-scaled internal resolution, ping-pong history, scene.frag ambient hook. Record in notes.md.", "status": "done", "result": "Mapping recorded in notes.md: VulkanGiPass in pass/gi/ registered after vulkanSsrPass before vulkanAOPass, half-internal-res estimate + full-res temporal history, ping-pong, scene.frag/azgaar_props.frag ambient hook with one-frame latency + NULL sentinel, AO attenuation via CACAO shadowMultiplier setter."},
  {"id": 3, "desc": "Write plans/ssgi.md: status header, scope, algorithm overview, phased plan (P1 estimate+debug vis pass, P2 temporal, P3 spatial + ambient injection, P4 vegetation/props + FSR reactive-mask validation), per-phase deliverables, cost budget, and risks/unknowns — matching structure of existing plans (e.g. plans/ambient-occlusion.md, plans/heightmap-terrain.md). While writing, finish the 4 residual fact checks the brainstorm flagged: (a) FSR reactive-mask section of docs/fsr3.1.md, (b) how scripts/build.sh globs new .comp sources into the pak, (c) VulkanAOPass temporal push-constant struct fields, (d) vulkanAOPassGetOutput NULL-sentinel contract. Use notes.md as the substrate; note any deviation from the survey explicitly.", "status": "done", "result": "plans/ssgi.md written (457 lines): status checklist, background, survey corrections, 7 pinned design decisions, P1–P4 with per-phase verification commands, cost budget, risks; residual fact checks (a)-(d) resolved against tree (reactive.comp is the real FSR reactive source; no build-system edits needed; AoTemporalPushConstants 13 fields; GetOutput NULL-sentinel contract adopted)."},
  {"id": 4, "desc": "Cross-check every file path, pass name, G-buffer slot, and shader location cited in plans/ssgi.md against the actual tree (Vulkan.cpp pass list, VulkanFrameResources.cpp, scene.frag, AO pass); fix any stale or invented references.", "status": "done", "result": "Round 2 cross-check fixed 14 stale/invented references (ao.comp attribution, blue-noise unpopulated, GI pass index 11→18, line-range and VRAM-math corrections, ENGINE_FSR env var absent, sceneBuffer slot premise, XeGTAO numbers labeled historical); verifier PASS."}
]
